import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Calculadora_Mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
